# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.10.0

# HTTP client for LLM API
httpx>=0.27.0

# OpenAI SDK for Whisper STT and Chat Completions
openai>=1.0.0

# Fish Audio SDK for TTS
fish-audio-sdk>=0.1.0

# Audio processing (for converting formats if needed)
pydub>=0.25.1

# No LiveKit dependencies - removed as requested

